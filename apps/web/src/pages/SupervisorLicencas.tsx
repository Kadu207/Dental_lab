import { Navigate } from "react-router-dom";

/** Rota legada — conteúdo unificado em Cadastro de empresas. */
export default function SupervisorLicencasPage() {
  return <Navigate to="/supervisor/tenants" replace />;
}
